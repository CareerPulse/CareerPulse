import {Container, TextField} from "@mui/material";

const SearchResultPage = () => {

    return (
        <Container>
            <TextField>Search result</TextField>
        </Container>
    );
};

export default SearchResultPage;
